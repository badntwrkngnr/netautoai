apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hermes-dashboard
  namespace: automation
spec:
  entryPoints: [hermes]
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: hermes-agent
          port: 9119
